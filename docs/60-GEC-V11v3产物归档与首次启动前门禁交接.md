# GEC V11 v3 产物归档与首次启动前门禁交接

更新日期：2026-09-04

## 1. 归档结果

主机侧 v3 产物已归档到 Windows 共享目录：

```text
/mnt/hgfs/WinDownloads/rkav-gec-v11-wired-v3-20260904-102917.tar.gz
大小：26297125 字节
SHA-256：74863afdfff46711624eaefd4ef4ddc2cd395474d8d82f44055ece5ec307dcff
```

归档清单包含 v3 DTS、DTB、kernel Image、FIT `boot.img`、候选 defconfig、`output/.config`、SDK 构建
日志、`MANIFEST.txt` 和 `SHA256SUMS`。归档命令返回 0，`BUNDLE_GATE=PASS`。

## 2. 关键哈希

- v3 DTB：185071 字节，SHA-256 `0a89ede66f671140a2b9637f3e936bce865fb1e94112dafde93bd37e63d0f8f2`；
- FIT boot：37520896 字节，SHA-256 `a5b893eed11ffc6cb639166c1fd543bdd2b6fc59d84cacc7c1c24d732c0e0039`；
- kernel Image：37102080 字节，SHA-256 `e66dca717b321e0c8c4b74aa67786cc97d3c642de7896af14eb6b4b7e9543b86`；
- SDK 构建日志 SHA-256 `50ce8379ee6cb1155d825e010cebb0df407be9c8d76dda606f12c6ed65db6270`。

Image 与之前候选相同是正常增量结果，因为本轮只改变设备树；v3 DTB 和 FIT boot 均为本轮生成。

## 3. 当前边界

归档成功只证明主机侧候选可回退、可传输和可复核，不证明开发板已启动 5.10。原 SDK 工作树仍干净；隔离
device/kernel 的候选文件与 `.version` 构建改动保留。未部署、未刷写、未操作开发板。

## 4. 下一步

首次上板前仍需用户明确确认。确认后先执行板端只读进程、设备、存储、串口和电源状态检查，再决定是否
进行低风险启动验证；不直接启动网关、不启动高负载测试，不删除旧 4.19 回退证据。
