# 项目当前开发状态与验收记录

更新日期：2026-09-02

## 1. 当前一句话结论

项目已经从纯 Mock 基线推进到真实摄像头、真实麦克风和 RKNN NPU 分阶段验收：绿联 USB
摄像头与麦克风可用，RKNN Runtime 1.4.0、MobileNet 和 RK3568 YOLOv5s INT8 均已在
板端独立运行，主程序的 `RknnInferenceEngine` 也已在板端完成 Mock RGB + RKNN 10 秒验证。
当前已新增基于 libjpeg-turbo 的 MJPEG 解码预处理，并使用真实 UGREEN 1280x720 JPEG 样本
完成 Windows Debug/Release 回归。新产物也已完成 AArch64 交叉构建、板端真实
`/dev/video9` MJPEG + RKNN 10 秒/60 秒验证，以及真实 MJPEG + ALSA + RKNN 10 秒联合
验收。启动阶段 ALSA XRUN 已定位并修复；30 分钟三硬件联合长稳已完成，网关正常退出且业务、
NPU、资源和队列指标全部通过。原控制器因误把解码延迟滑动窗口的 3,594 个样本当成累计解码
次数而写出 `failed:1`；原始证据已保留并完成 SHA-256 校验，按累计推理次数重验收后结论为通过。
随后 Ubuntu 主机上的 FFmpeg 软件媒体基线已生成并校验真实 H.264/AAC MP4：5.020 秒、
320x180、48 kHz 双声道，PTS/DTS 单调，音视频起始 PTS 均为零。其后 CPU OSD 已在新的
5 秒软件 MP4 验收中通过：151 帧均完成精确来源帧检测框和文字叠加，零跳过、零错误和零队列丢弃。
MPP/RGA 候选后端已通过 Ubuntu 22.04 容器 AArch64 构建，最高只要求 `GLIBC_2.34`，低于板端
2.35。2026-09-02 已在唯一结果目录完成首次板端 ABI 短测：V4L2 和 ALSA 均成功打开，但 MPP 在
H.264 编码配置阶段返回 `-1` 并安全退出，未采集、推理、编码或生成 H.264。公开头文件新候选未
通过旧 BSP 运行时兼容门禁；原始失败证据已锁定。LubanCat 5.10.209 迁移的公开性评估已经完成：
官方 Generic Full 归档已下载、校验并在 Ubuntu 离线检出 51/51 个项目。GecEdu 公开仓库还提供厂商
镜像反编译 DTS、5.10 配置片段、DTB 对比脚本和已验证板级差异记录；但最终 GEC DTS/DTSI、完整
defconfig 和可重放 patch 仍未公开，不能直接视为 GEC 已适配。

## 2. 已完成的工程基线

- 主链路包含视频、音频、推理、Checksum 编码、包分发、独立 Sink、结构化日志、指标和健康检查。
- 所有线程间队列都有固定容量、超时、丢弃策略和高水位指标。
- 视频允许按策略丢旧帧；音频默认不静默丢块。
- 统一使用单调微秒时间域，推理结果保留来源帧 sequence 和 PTS。
- 正常停止按采集、处理、Sink、后端的顺序排空；必需 Sink 失败会停止管道。
- 可选 FFmpeg 后端已实现 H.264 软件视频编码、AAC 音频编码和 MP4 封装；正常完成时先写
  `.part` 临时文件，成功写入 trailer 后才原子改名为最终 MP4，避免把不完整文件当成成品。
- CPU OSD 已使用独立 RGB/BGR Buffer 绘制检测框与类别/置信度文字；它只接受同一来源序号和
  PTS 的检测结果，并以有限等待避免阻塞视频线程。
- Mock 音频 XRUN、设备重试、无进展健康检查和信号退出均有测试覆盖。
- 主程序接入 JPEG 解码后，单元与集成测试增加到 43 项；Windows Debug、Release 和真实
  摄像头 JPEG 样本均已通过。既有 GitHub CI 的 Debug、Sanitizers、静态分析和 RKNN
  AArch64 交叉构建证据对应解码接入前基线；当前 JPEG/ALSA 修复产物也已单独通过 AArch64
  交叉构建、动态依赖和 GLIBC 兼容门禁。

早期 30 项板端测试、Mock 长稳和 Buildroot 部署证据仍有效，详见
[Buildroot 交叉编译与 RK3568 开发板部署总结](09-Buildroot交叉编译与RK3568开发板部署总结.md)。

## 3. RK3568 板端基线

| 项目 | 已确认结果 |
|---|---|
| SoC/架构 | RK3568，AArch64 |
| 系统 | Buildroot 2018.02-rc3-dirty |
| 内核 | Linux 4.19.232 |
| glibc | 2.35 |
| RKNN Runtime | 1.4.0 |
| NPU 驱动 | 0.8.2 |
| Runtime | `/usr/lib/librknnrt.so`，MD5 `757e7f089e657a7fc28c67c71738f0cc` |
| NPU 节点 | `/dev/dri/card1`、`/dev/dri/renderD129` |
| TF 卡 | `/dev/mmcblk1p1` 自动挂载到 `/media/sdcard0`，挂载参数包含 `noexec` |

板端没有 GCC、CMake、`file` 和 `readelf`，所以正式流程是在 x86_64 Ubuntu 中交叉编译，
通过 TF 卡部署后在板端运行。大文件 TFTP 曾触发旧网卡驱动 watchdog，本阶段不再使用网络
传输大产物。

## 4. 真实硬件验收状态

### 4.1 摄像头与麦克风

- 绿联 USB 摄像头已识别并完成真实采集验证，目标视频节点为 `/dev/video9`。
- 摄像头实际输出为 1280x720 MJPEG，不是可直接送入 RKNN 的裸 RGB。
- 已完成单帧 MJPEG 解码、RGB 转换、模型尺寸缩放和原始 RGB 输入生成。
- USB 麦克风已完成 ALSA 枚举、真实录音和 30 分钟音视频联调。
- 三硬件第一次联合运行暴露出 ALSA 在 RKNN 初始化前过早启动导致的启动 XRUN；现已把 PCM
  `START` 和首个 PTS 推迟到第一次 `Read()`，修复版 10 秒取得 500 个音频块、零恢复和零
  工作线程错误。
- 真实摄像头、ALSA、RKNN 三硬件 30 分钟连续运行已通过。
- 带 RKNN 链路的 USB 断连和重插恢复已通过：断连后约 3.27 秒自行安全退出，重插后节点恢复，
  10 秒重启取得 279 视频帧、500 音频块和 49 次推理/NPU 中断，零错误/恢复/工作线程错误。
- 当前降级策略是必需采集设备丢失后安全退出，再由人工或未来 systemd 重启；进程内自动热重连
  尚未实现。

### 4.2 RKNN 与 YOLOv5

- 已固定 RKNN 1.4.0 的 `rknn_api.h` 和 RK356X AArch64 `librknnrt.so` 开发文件及哈希。
- C17 `rknn-smoke` 已在板端完成 MobileNet 单次和 100 次推理。
- RK3568 YOLOv5s INT8 模型已明确使用 `target_platform=rk3568` 转换并校验。
- C17 YOLOv5 后处理已实现三尺度解码、阈值过滤、逐类别 NMS 和坐标还原。
- 固定 `bus.jpg` 已得到 4 个 person 和 1 个 bus；真实摄像头单帧也已完成板端 NPU 检测。
- 连续 10 次真实输入推理无新增 NPU/IOMMU 错误，最后一次核心 NPU 耗时约 93.4 ms。

详细证据见
[RKNN 运行时独立冒烟测试](15-RKNN运行时独立冒烟测试.md)和
[RK3568 YOLOv5 目标检测验证](16-RK3568-YOLOv5目标检测验证.md)。

## 5. 主程序 RKNN 接入状态

提交 `6cefd51` 已实现：

- `RknnInferenceEngine` 和厂商 SDK 隔离层；
- RKNN 模型输入输出校验、RGB/BGR 缩放、推理和输出释放；
- YOLOv5 解码、NMS、来源坐标映射、frame sequence 与 PTS 绑定；
- `backend=rknn` 工厂选择和模型、阈值、最大帧率等配置；
- 推理队列前限帧，当前建议为 5 FPS、容量 1、`keep_latest`；
- 固定 Ubuntu 22.04、GCC/G++ 11.4.0 的 AArch64 容器构建；
- ELF 架构、动态依赖和最高 GLIBC 版本门禁。

GitHub CI 已证明 AArch64 `rkav-gateway` 最高只要求 GLIBC 2.34，低于板端 2.35。
提交 `8c95223` 的本地产物随后已部署到板端：10 秒内完成 51/51 次推理，NPU 中断从 10
增加到 61，错误、恢复、队列丢弃和新增相关内核日志均为 0，温度从 47.777 升至 50.000 摄氏度。
证据目录为 `/userdata/rkav/gateway-8c95223/mock-rgb-rknn-10s`。

当前工作区进一步实现了：

- `IVideoDecoder` 稳定接口和 libjpeg-turbo 3.1.4.1 `JpegVideoDecoder`；
- JPEG 头尺寸、最大像素数、整数溢出和分配失败保护；
- 坏 JPEG 作为可恢复帧错误跳过，同一解码器可继续处理后续帧；
- 只在推理抽帧后解码，保留原始 MJPEG 给视频编码分支；
- `video_decode` 延迟指标和 `rk3568-rknn-mjpeg.json` 板端配置。

## 6. 当前精确停止位置

| 环节 | 状态 |
|---|---|
| `8c95223` AArch64 主程序构建和部署 | 已完成 |
| 板端 Mock RGB + RKNN 10 秒测试 | 已通过 |
| MJPEG 解码接口和 libjpeg-turbo 接入 | 已完成 |
| Windows Debug/Release 43 项测试 | 已通过 |
| 真实 1280x720 摄像头 JPEG 解码 | 已通过 |
| 新代码 AArch64 交叉构建与 ABI 门禁 | 已通过，最高要求 GLIBC 2.34 |
| 真实 `/dev/video9` MJPEG + 主程序 RKNN 10 秒 | 已通过 |
| 真实 MJPEG + RKNN 60 秒稳定性 | 已通过 |
| 真实 MJPEG + ALSA + RKNN 10 秒联合测试 | 已通过：276 视频帧、500 音频块、49 次 NPU 推理，零错误/恢复/丢弃 |
| ALSA 延迟启动修复版 | 已部署并通过，ELF SHA-256 `9d29d432...da6623` |
| 真实三硬件 30 分钟长稳 | 修正规则后通过：50,237 视频帧、89,983 音频块、8,714 次推理/NPU 中断，零错误/恢复/丢弃/残留；RSS 57,652 KiB、线程 9、峰值 57.222 摄氏度 |
| 真实 RKNN 链路 USB 断连 | 已通过：UVC/UAC 同时消失，约 3.27 秒自行退出，退出码 1，`forced_stop=0`，队列零丢弃/残留 |
| USB 重插后 10 秒重启 | 已通过：279 视频帧、500 音频块、49 次推理和 NPU 中断，779 个包全部消费，零错误/恢复/工作线程错误 |
| Ubuntu FFmpeg 软件 MP4 基线 | 已通过：Mock 源生成 5.020 秒 H.264/AAC MP4，`ffprobe` 校验 320x180、48 kHz 双声道、PTS/DTS 单调、起始音画偏移 0 ms；零错误、队列丢弃和残留 |
| Ubuntu CPU OSD 软件 MP4 | 已通过：151 视频帧和 151 推理结果均完成精确来源帧 OSD，p99 260 us，H.264/AAC MP4 时长 5.040 秒，零跳过/错误/恢复/队列丢弃和残留 |
| MPP/RGA 候选后端源码与原生 AArch64 编译 | 源码 40/40 编译和链接通过；ELF 为 AArch64，但 Ubuntu 24.04 GCC 13 产物要求 GLIBC 2.38，被 2.35 上限门禁拒绝，未部署 |
| Ubuntu 22.04 MPP/RGA 容器兼容构建（原候选） | 已通过：GCC/G++ 11.4，AArch64，最高 GLIBC 2.34；原结果目录后来被清理，旧候选 ELF 不再可用 |
| Ubuntu 22.04 MPP/RGA 容器兼容构建（新公开输入候选） | 已通过：MPP `0986d012...33967`、RGA `2b32edcb...6d55`、nlohmann/json `9cca280...8d03`；40/40、最高 GLIBC 2.34；ELF SHA-256 `86131dcc...e289e` |
| 首次板端 MPP/RGA ABI 短测 | 未通过：唯一目录 `/userdata/rkav/mpp-rga-abi-19700101-021545-755`；V4L2 `/dev/video9` 与 ALSA `hw:2,0` 均打开，MPP 配置阶段返回 `-1`；退出码 1，业务计数均为 0，未生成 H.264；`failure-evidence.sha256` 14/14 `OK` |
| LubanCat 5.10.209 公开 SDK 与 GEC V11 迁移评估 | 已下载并只读核验官方 Generic Full `20260729`：7,122,238,264 字节，SHA-256 `57A61FD3...F78473FF7`；Ubuntu 离线检出 51/51 项目。GecEdu `4f9cd0af...` 提供 factory DTS、配置片段和板级差异记录，但没有最终 GEC DTS/DTSI、完整 defconfig 或 patch；未构建、未上板 |
| GEC V11 DTB 可复核差异基线 | 已通过：隔离目录 `rkav-gec-dtb-rebuild-20260902-213048` 中，5.10 EVB1 DTB 编译和两份 DTB 反编译均为退出码 0；标准 `diff` 生成 262767 字节差异文本（退出码 1 表示不同）。官方 DTB、factory DTB、DTS、日志和差异文件均已锁定 SHA-256；尚待板级差异分类和候选 DTS |

最新的完整证据、哈希、ALSA 启动修复和 30 分钟验收规则修正见
[真实三硬件联合验收与 ALSA 启动修复总结](23-真实三硬件联合验收与ALSA启动修复总结.md)。
USB 断连、重插重启和恢复语义见
[USB 断连与重插恢复验收](24-USB断连与重插恢复验收.md)。
FFmpeg 软件 MP4 的实现、Ubuntu 验收证据和边界见
[FFmpeg 软件 MP4 基线验收与交接](25-FFmpeg软件MP4基线验收与交接.md)。
[CPU OSD 软件 MP4 验收与交接](26-CPU-OSD软件MP4验收与交接.md)记录当前最新软件媒体阶段。
[MPP/RGA 运行时盘点与 SDK 门禁](27-MPP-RGA运行时盘点与SDK门禁.md)记录本阶段开始时已确认的
板端运行环境和当前 SDK 前置条件。
[MPP/RGA 候选后端实现与兼容构建交接](28-MPP-RGA候选后端实现与兼容构建交接.md)记录当前精确
停止位置、原生构建门禁失败证据和容器构建入口。
[本次会话实现总结、问题归档与下一步交接](29-本次会话实现总结、问题归档与下一步交接.md)保留
短测前的容器候选构建记录；[MPP/RGA 首次板端 ABI 短测失败与下一步交接](30-MPP-RGA首次板端ABI短测失败与下一步交接.md)
记录首次失败；[本次板端准备、ABI 短测与下一步交接](31-本次板端准备、ABI短测与下一步交接.md)
保留板端操作记录；[LubanCat 5.10.209 SDK 公开性与 GEC V11 迁移评估](32-LubanCat-5.10.209-SDK公开性与GEC-V11迁移评估.md)
是当前最新交接。
较早的 RKNN 接入过程仍见
[本次会话实现总结与下一步](18-本次会话实现总结与下一步.md)。

## 7. 下一步执行顺序

1. 30 分钟真实三硬件长稳、USB 断连/重插恢复、Ubuntu FFmpeg 软件 MP4 和 CPU OSD 基线均已归档，
   不重复运行。
2. 新公开 MPP/RGA 头文件候选已完成板端短测但在 MPP H.264 配置阶段被旧 BSP 运行库拒绝；不重跑、
   不猜测修改配置键，也不以 GLIBC 通过宣称 ABI 匹配。
3. 优先推进 LubanCat 5.10.209 路线：官方 Generic Full 归档已在 Ubuntu 离线检出为完整工作树；其中
   kernel-5.10 为 5.10.209，Full 媒体/NPU external 锁在私有 `linux-6.1-stan-rkr5`。GecEdu 公共资料已
   补到 factory DTS、配置片段、include 链和五批板级差异，但最终 GEC 源文件仍缺失；主机侧 EVB1/factory
   旧 `205443` 分析目录的成功摘要无法由保留产物复核，已经降级；新的可复核 DTB 基线已生成。
   已完成首轮分类：DSI0 面板、GMAC1 和 Wi-Fi 为明确板级候选，USB/eMMC/SDMMC 尚需核对源节点，
   媒体/NPU phandle 差异暂不复制。5.10 源码中的 GMAC/DSI 覆盖标签已确认；仅含 GEC model 与
   GMAC1 参数的有线管理候选 DTS 已通过独立 DTC 编译/反编译门禁。下一步创建隔离 kernel worktree，
   让候选进入 kernel Makefile 的正式 DTB 构建路径；不冒充原厂配置。
   用户态 MPP/RGA/RKNN 版本和 sysroot 仍需按 5.10 内核驱动另行锁定。
   当前 4.19 的匹配 SDK 仍是可并行保留的低改动备选，但不能与 5.10 组件混装。
4. 完成离线镜像与 ABI 检查后，只有经用户明确确认才做首次 5.10 启动；先只读枚举，不启动网关。
5. 只有匹配 SDK 候选能产生并验证 H.264 elementary stream 后，才进入 RTSP 与网络恢复；仍不把
   elementary stream 写成 MP4、AAC 或 RTSP 成品。
6. 最后完成 systemd、SIGTERM、日志轮转及 2 小时/
   12 小时长稳。

## 8. 尚未关闭的系统问题

- Dropbear TCP 22 可达，但 OpenSSH banner 曾超时，SSH/SCP 仍未正式验收。
- RTC 没有注册设备节点，`pcf8563@0x51` probe 失败，冷启动后仍需临时校时。
- 厂商 `reboot` 曾停在 `usbdevice stop` 附近；`poweroff` 已成功，重启根因未关闭。
- NPU 开机日志含功耗模型 warning；推理期间没有新增，但设备树问题仍是残余风险。
- 默认 Checksum 编码器仍不是 H.264/AAC；已新增 Ubuntu Mock 源的 FFmpeg H.264/AAC MP4
  软件基线和 CPU OSD，但尚未在 RK3568 上做 FFmpeg/MPP 实机媒体输出、真实 OSD 或 RTSP 成品。
- MPP/RGA 新公开头文件候选虽已通过 GLIBC 2.35 容器门禁，但首次板端短测在 MPP H.264 编码配置阶段
  返回 `-1`，未生成 H.264；这证明当前公开头文件与 `RK356X_Linux_V1.3.2` 旧运行库的 ABI/配置
  语义尚未匹配。原始证据已锁定，详见 P068、P091、
  [MPP/RGA 运行时盘点与 SDK 门禁](27-MPP-RGA运行时盘点与SDK门禁.md)和
  [MPP/RGA 首次板端 ABI 短测失败与下一步交接](30-MPP-RGA首次板端ABI短测失败与下一步交接.md)。
- LubanCat 5.10.209 内核和 Generic Full 网盘归档均可公开取得；Ubuntu 工作树已核验 MPP/RGA/RKNN
  版本和头文件。GecEdu 公共仓库已有 factory DTS、配置片段和移植证据，但公开树及归档工作树都没有
  最终 GEC V11 DTS/DTSI、完整 defconfig 或可重放 patch，详见 P095 和
  [LubanCat 5.10.209 SDK 公开性与 GEC V11 迁移评估](32-LubanCat-5.10.209-SDK公开性与GEC-V11迁移评估.md)。
- GEC V11 DTB 现有可复核基线来自 `213048` 隔离目录：官方 5.10 EVB1 DTB、公开 factory DTB、
  两份反编译 DTS 和 262767 字节差异文本均已保存并哈希。此前 `205443` 目录的成功摘要没有留下
  官方 DTB 和差异产物，不能独立复核，已按 P098 记录；第二输入仍不是最终 GEC 5.10 DTB，仍需
  核对存储、USB 和无线等板级候选。第一轮已确认 DSI0 面板和 GMAC1 的实际配置与官方 EVB1 不同；
  有线管理候选已通过独立 DTC 编译、反编译和属性断言，详见
  [GEC V11 GMAC1 候选 DTS 离线验证与正式内核构建交接](36-GEC-V11-GMAC1候选DTS离线验证与正式内核构建交接.md)。
- MJPEG、真实 ALSA 和实时 NPU 已完成 30 分钟长稳及 USB 断连/重插重启；这不等于进程内
  自动热重连、2 小时/12 小时最终长稳或真实媒体输出已经完成。
- 散热片已经安装；风扇计划更换为独立 USB 供电的 5V、约 40x40x10 mm 规格。

## 9. 相关文档

- [USB 断连与重插恢复验收](24-USB断连与重插恢复验收.md)
- [FFmpeg 软件 MP4 基线验收与交接](25-FFmpeg软件MP4基线验收与交接.md)
- [CPU OSD 软件 MP4 验收与交接](26-CPU-OSD软件MP4验收与交接.md)
- [MPP/RGA 运行时盘点与 SDK 门禁](27-MPP-RGA运行时盘点与SDK门禁.md)
- [MPP/RGA 候选后端实现与兼容构建交接](28-MPP-RGA候选后端实现与兼容构建交接.md)
- [本次会话实现总结、问题归档与下一步交接](29-本次会话实现总结、问题归档与下一步交接.md)
- [MPP/RGA 首次板端 ABI 短测失败与下一步交接](30-MPP-RGA首次板端ABI短测失败与下一步交接.md)
- [本次板端准备、ABI 短测与下一步交接](31-本次板端准备、ABI短测与下一步交接.md)
- [LubanCat 5.10.209 SDK 公开性与 GEC V11 迁移评估](32-LubanCat-5.10.209-SDK公开性与GEC-V11迁移评估.md)
- [GEC V11 DTB 可复核差异基线与板级候选分类交接](34-GEC-V11-DTB可复核差异基线与板级候选分类交接.md)
- [GEC V11 板级差异首轮分类与最小候选构造交接](35-GEC-V11板级差异首轮分类与最小候选构造交接.md)
- [GEC V11 GMAC1 候选 DTS 离线验证与正式内核构建交接](36-GEC-V11-GMAC1候选DTS离线验证与正式内核构建交接.md)
- [真实三硬件联合验收与 ALSA 启动修复总结](23-真实三硬件联合验收与ALSA启动修复总结.md)
- [本次会话实现总结与下一步](18-本次会话实现总结与下一步.md)
- [MJPEG 解码预处理接入与验收](22-MJPEG解码预处理接入与验收.md)
- [RKNN YOLOv5 主程序后端接入](17-RKNN-YOLOv5主程序后端接入.md)
- [真实音视频联调总结与下一步](12-真实音视频联调总结与下一步.md)
- [项目问题汇总：面试版](06-项目问题汇总-面试版.md)
- [项目问题汇总：通俗版](07-项目问题汇总-通俗版.md)
